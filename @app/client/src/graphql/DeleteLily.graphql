mutation Delete($id: Int!) {
  deleteLily(input: { id: $id }) {
    user {
      lilies {
        nodes {
          id
          name
          price
          note
          updatedAt
        }
      }
    }
  }
}
