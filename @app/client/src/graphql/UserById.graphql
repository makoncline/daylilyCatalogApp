query UserById($id: Int!) {
  ...SharedLayout_Query
  user(id: $id) {
    id
    ...UserData
    lilies {
      nodes {
        id
        ...LilyData
      }
    }
    lists {
      nodes {
        id
        ...ListData
      }
    }
  }
}
