mutation CreateStripeCustomer($stripeId: String) {
  createStripeCustomer(input: { stripeCustomer: { stripeId: $stripeId } }) {
    user {
      id
      stripeCustomer {
        stripeId
      }
    }
  }
}
