#import "./AhsData.graphql"
#import "./ListData.graphql"

query LilyById($id: Int!) {
  lily(id: $id) {
    id
    ...LilyData
    ahsDatumByAhsRef {
      id
      ...AhsData
    }
    list {
      id
      ...ListData
    }
  }
}
